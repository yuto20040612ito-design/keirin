# 収集データは再生成不能。手元やサーバーで動かす場合は Git に入れず別途バックアップする。
# GitHub Actions で収集する場合だけは例外で、ワークフローが -f を付けて
# data/raw と data/state をコミットする(データの置き場がそこしか無いため)。
data/
*.duckdb
*.duckdb.wal
*.parquet

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
